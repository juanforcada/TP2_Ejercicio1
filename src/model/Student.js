export class Student {
  constructor({ id, userCode, nombreCompleto, notas }) {
    this.id = id;
    this.userCode = userCode;
    this.nombreCompleto = nombreCompleto;
    this.notas = notas;
  }
}
